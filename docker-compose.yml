version: '3.8'

services:
  clr-trading-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clr_bingx_bot
    restart: always
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - ./data:/app/backend/data
